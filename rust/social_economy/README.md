# Social Economy Scoring (Rust)

PyO3-based helper crate that provides lightweight scoring functions for content engagement and quality.

## Build

```bash
cargo build --release
```

## Python Integration

The crate is intended to be built as a Python extension (e.g., via maturin) and imported from the app's social economy layer.
